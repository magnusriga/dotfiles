#  _                      _            _
# | |__  _   _ _ __  _ __| | ___   ___| | __
# | '_ \| | | | '_ \| '__| |/ _ \ / __| |/ /
# | | | | |_| | |_) | |  | | (_) | (__|   <
# |_| |_|\__, | .__/|_|  |_|\___/ \___|_|\_\
#        |___/|_|
#

# Own config.
source = $HOME/.config/hypr/mocha.conf
$accent = $mauve
$accentAlpha = $mauveAlpha
$font = JetBrainsMono Nerd Font

# Cache folder
$my_cache_folder = $HOME/.cache/my/hyprland-dotfiles

# Source colors
source = colors.conf

general {
    ignore_empty_input = true
    # disable_loading_bar = true # Does not exist?
    hide_cursor = true
}

background {
    monitor =
    # path = ~/.config/my/wallpapers/shaded.png
    path = $my_cache_folder/current_wallpaper.png  # only png supported for now
    blur_passes = 0 # Use 2 for blurred, but 0 is gorgeous.
    color = $base

    # From ml4w, but prefer `blur_passes = 2`.
    # path = $my_cache_folder/blurred_wallpaper.png   # only png supported for now
}


# TIME
label {
    monitor =
    text = cmd[update:30000] echo "$(date +"%R")"
    color = $text
    font_size = 90
    font_family = $font
    position = -30, 0
    halign = right
    valign = top
}

# DATE 
label {
    monitor = 
    text = cmd[update:43200000] echo "$(date +"%A, %d %B %Y")"
    color = $text
    font_size = 25
    font_family = $font
    position = -30, -150
    halign = right
    valign = top
}

# HEADSHOT
image {
    monitor = 
    path = ~/.config/my/headshots/default.jpg # Or: `avatar.png`.
    size = 200
    border_color = $accent

    position = 0, 140
    halign = center
    valign = center
}

# INPUT FIELD
input-field {
    monitor =
    size = 300, 60
    outline_thickness = 4
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    outer_color = $accent
    inner_color = $surface0
    font_color = $text
    fade_on_empty = false
    placeholder_text = <span foreground="##$textAlpha"><i>󰌾 Logged in as </i><span foreground="##$accentAlpha">$USER</span></span>
    hide_input = false
    check_color = $accent
    fail_color = $red
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    capslock_color = $yellow
    position = 0, -35
    halign = center
    valign = center
}

# ================================
# From ml4w, but prefer own.
# ================================
# input-field {
#     monitor =
#     size = 200, 50
#     dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
#     dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
#     dots_center = true
#     dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
#     inner_color = $primary
#     font_color = $on_primary
#     font_family = Fira Semibold
#     outer_color = $on_primary
#     outline_thickness = 3
#     fade_on_empty = true
#     fade_timeout = 1000 # Milliseconds before fade_on_empty is triggered.
#     placeholder_text = <i>Input Password...</i> # Text rendered in the input box when it's empty.
#     hide_input = false
#     rounding = 10 # -1 means complete rounding (circle/oval)
#     check_color = $primary
#     fail_color = $error # if authentication failed, changes outer_color and fail message color
#     fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty
#     capslock_color = -1
#     numlock_color = -1
#     bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
#     invert_numlock = false # change color if numlock is off
#     swap_font_color = false # see below
#     position = 0, -20
#     halign = center
#     valign = center
#     shadow_passes = 10
#     shadow_size = 20
#     shadow_color = $shadow
#     shadow_boost = 1.6
# }

# label {
#     monitor =
#     #clock
#     text = cmd[update:1000] echo "$TIME"
#     color = $foreground
#     font_size = 55
#     font_family = Fira Semibold
#     position = -100, 70
#     halign = right
#     valign = bottom
#     shadow_passes = 5
#     shadow_size = 10
# }

# label {
#     monitor =
#     text = $USER
#     color = $foreground
#     font_size = 20
#     font_family = Fira Semibold
#     position = -100, 160
#     halign = right
#     valign = bottom
#     shadow_passes = 5
#     shadow_size = 10
# }

# image {
#     monitor =
#     path = $my_cache_folder/square_wallpaper.png
#     size = 280 # lesser side if not 1:1 ratio
#     rounding = 40 # negative values mean circle
#     border_size = 4
#     border_color = $primary
#     rotate = 0 # degrees, counter-clockwise
#     reload_time = -1 # seconds between reloading, 0 to reload with SIGUSR2
# #    reload_cmd =  # command to get new path. if empty, old path will be used. don't run "follow" commands like tail -F
#     position = 0, 200
#     halign = center
#     valign = center
#     shadow_passes = 10
#     shadow_size = 20
#     shadow_color = $shadow
#     shadow_boost = 1.6
# }
