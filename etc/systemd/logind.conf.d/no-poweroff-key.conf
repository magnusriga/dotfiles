# Disable systemd-logind's reaction to the keyboard's power key (KEY_POWER).
# Some keyboards (e.g. HJKB) emit KEY_POWER on Fn+Esc and would otherwise
# trigger an immediate shutdown via the default HandlePowerKey=poweroff.

[Login]
HandlePowerKey=ignore
HandlePowerKeyLongPress=ignore
